<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :business_nbame %>
    <%= form.text_field :business_nbame %>
  </div>

  <div class="field">
    <%= form.label :gst_no %>
    <%= form.number_field :gst_no %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
